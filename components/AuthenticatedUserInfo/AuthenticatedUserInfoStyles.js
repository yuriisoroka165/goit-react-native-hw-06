import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    userContainer: {
        height: 60,
        marginTop: 32,
        marginBottom: 16,
        paddingLeft: 16,
        paddingRight: 16,
        display: "flex",
        flexDirection: "row",
        gap: 8,
        alignItems: "center",
    },
});
